{
  "name": "time-scale",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "dev": "npm run build && npm run serve",
    "preview": "npm run build && npm run serve",
    "serve": "node scripts/serve-site.mjs --root _site --host 0.0.0.0 --port 8000",
    "test": "node --test"
  }
}
